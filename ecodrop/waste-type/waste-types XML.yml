info:
  name: waste-types XML
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types
  headers:
    - name: Accept
      value: application/xml
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
