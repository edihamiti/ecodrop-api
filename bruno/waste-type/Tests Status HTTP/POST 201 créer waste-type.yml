info:
  name: POST 201 - créer waste-type
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/ecodrop/waste-types
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "nom": "Test Status",
        "pointsPerKilo": 5
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

