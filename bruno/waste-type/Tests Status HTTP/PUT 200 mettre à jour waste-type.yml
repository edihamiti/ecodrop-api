info:
  name: PUT 200 - mettre à jour waste-type
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/ecodrop/waste-types/1
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": 1,
        "nom": "Plastique modifié",
        "pointsPerKilo": 10
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
