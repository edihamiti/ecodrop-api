info:
  name: GET 400 - waste-type ID non valide
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/afd
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

