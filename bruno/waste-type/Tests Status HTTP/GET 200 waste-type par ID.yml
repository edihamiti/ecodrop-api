info:
  name: GET 200 - waste-type par ID existant
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/1
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

