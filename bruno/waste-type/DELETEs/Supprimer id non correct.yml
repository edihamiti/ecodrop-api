info:
  name: Supprimer id non correct
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/ecodrop/waste-types/fds
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
