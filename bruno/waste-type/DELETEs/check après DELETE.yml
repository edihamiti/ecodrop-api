info:
  name: check après DELETE
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/13
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
