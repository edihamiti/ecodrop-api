info:
  name: ajouter type déchets
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/ecodrop/waste-types
  headers:
    - name: Accept
      value: application/xml
  body:
    type: json
    data: |-
      {
        "id": 3,
        "nom":"enzo",
        "pointsPerKilo":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
