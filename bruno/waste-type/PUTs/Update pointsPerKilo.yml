info:
  name: Update pointsPerKilo
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/ecodrop/waste-types/13
  body:
    type: json
    data: |-
      {
        "id":13,
        "nom":"{{$randomFirstName}}",
        "pointsPerKilo":{{$randomInt}}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
