info:
  name: check après PUT
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/13
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
