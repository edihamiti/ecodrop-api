info:
  name: waste-types id°1
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/1
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
