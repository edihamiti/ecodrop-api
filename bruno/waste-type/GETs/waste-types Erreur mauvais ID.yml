info:
  name: waste-types Erreur mauvais ID
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/ecodrop/waste-types/afd
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
