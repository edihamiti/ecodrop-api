info:
  name: PUT 404 - ID inexistant
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/ecodrop/deposits/99999
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userId": 1,
        "point": { "id": 1 },
        "wasteType": { "id": 1 },
        "weight": 12.5
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

