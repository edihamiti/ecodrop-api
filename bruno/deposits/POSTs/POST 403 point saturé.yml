info:
  name: POST 403 - point saturé
  type: http
  seq: 5
http:
  method: POST
  url: http://localhost:8080/ecodrop/deposits/
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userId": 1,
        "point": { "id": 8 },
        "wasteType": { "id": 1 },
        "weight": 999999
      }
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
