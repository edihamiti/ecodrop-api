info:
  name: PATCH 200 - mettre à jour deposit
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8080/ecodrop/deposits/1
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "weight": 5.0
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

