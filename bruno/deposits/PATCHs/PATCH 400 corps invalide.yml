info:
  name: PATCH 400 - corps invalide
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8080/ecodrop/deposits/1
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: not valid json {{{
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
