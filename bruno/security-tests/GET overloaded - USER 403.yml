info:
  name: GET overloaded - USER 403
  type: http
  seq: 1
http:
  method: GET
  url: http://localhost:8080/ecodrop/points/overloaded
  headers:
    - name: Authorization
      value: Bearer {{userToken}}
    - name: Accept
      value: application/json
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"
