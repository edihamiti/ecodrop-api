info:
  name: POST waste-type - USER 403
  type: http
  seq: 4
http:
  method: POST
  url: http://localhost:8080/ecodrop/waste-types
  headers:
    - name: Authorization
      value: Bearer {{userToken}}
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "New Waste Type",
        "pointsPerKilo": 10
      }
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"
