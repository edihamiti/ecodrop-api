info:
  name: PATCH 400 - ID non valide
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8080/ecodrop/users/abc
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "login": "nouveauLogin"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
