info:
  name: PATCH 404 - ID inexistant
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:8080/ecodrop/users/99999
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "login": "nouveauLogin"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

