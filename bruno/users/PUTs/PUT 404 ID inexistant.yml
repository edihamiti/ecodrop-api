info:
  name: PUT 404 - ID inexistant user
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/ecodrop/users/99999
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "login": "fantome",
        "password": "fantome_mdp",
        "role": "USER"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
