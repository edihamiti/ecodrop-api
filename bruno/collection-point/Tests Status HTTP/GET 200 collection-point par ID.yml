info:
  name: GET 200 - collection-point par ID
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/ecodrop/points/1
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

