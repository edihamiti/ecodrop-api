info:
  name: DELETE 400 - clear ID non valide
  type: http
  seq: 15
http:
  method: DELETE
  url: http://localhost:8080/ecodrop/points/abc/clear
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
    - name: Accept
      value: application/json
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
