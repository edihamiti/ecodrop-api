info:
  name: GET 200 - points overloaded
  type: http
  seq: 18
http:
  method: GET
  url: http://localhost:8080/ecodrop/points/overloaded
  headers:
    - name: Accept
      value: application/json
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
