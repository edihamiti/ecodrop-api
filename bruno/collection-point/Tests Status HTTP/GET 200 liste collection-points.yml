info:
  name: GET 200 - liste collection-points
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/ecodrop/points
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

