info:
  name: GET 200 - collection-point avec waste-types imbriques
  type: http
  seq: 3
http:
  method: GET
  url: http://localhost:8080/ecodrop/points/1
  headers:
    - name: Accept
      value: application/json
  auth: inherit
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
