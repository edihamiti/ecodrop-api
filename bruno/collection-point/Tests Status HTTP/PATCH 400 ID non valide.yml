info:
  name: PATCH 400 - ID non valide
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:8080/ecodrop/points/abc
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "adresse": "12 Rue de la Paix",
        "capaciteMax": 500
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

