info:
  name: points JSON
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/ecodrop/points
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
