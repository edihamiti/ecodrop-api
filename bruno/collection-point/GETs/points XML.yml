info:
  name: points XML
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/ecodrop/points
  headers:
    - name: Accept
      value: application/xml
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
