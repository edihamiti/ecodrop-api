<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Ecodrop API</display-name>

    <!-- 
         L'ordre des filtres dans web.xml définit l'ordre d'exécution de la chaîne.
         On place le RateLimitFilter EN PREMIER pour rejeter les abus avant tout traitement coûteux.
    -->

    <filter>
        <filter-name>RateLimitFilter</filter-name>
        <filter-class>security.RateLimitFilter</filter-class>
    </filter>

    <filter>
        <filter-name>SecurityFilter</filter-name>
        <filter-class>security.SecurityFilter</filter-class>
    </filter>

    <!-- Mappings (L'ordre des mappings définit l'ordre d'application) -->
    
    <filter-mapping>
        <filter-name>RateLimitFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>SecurityFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>
